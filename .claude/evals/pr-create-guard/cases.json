[
  {
    "label": "non-pr command → pass through silently",
    "command": "git status",
    "expected_exit": 0
  },
  {
    "label": "gh pr create → blocks or emits checklist (never silent)",
    "command": "gh pr create --title test --body test",
    "expected_exit": 2,
    "expected_output": "PR CREATE BLOCKED",
    "note": "Blocks on zero diff, unpushed branch, or uncommitted changes. On a clean feature branch with pushed commits, exits 0 with a verification checklist instead. This case reliably blocks in repos with uncommitted eval files."
  }
]
