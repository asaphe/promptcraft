# tools/cursor/

> Placeholder — content lands in commit 3.

Cursor IDE configurations.

Planned subdirectories (populated via `git mv` in commit 2):

- `rules/user/` — markdown rules for Cursor's user-rules UI
- `rules/mdc/` — `.mdc` rules with frontmatter for `.cursor/rules/*.mdc`
- `mcp/` — MCP server setup and integration notes
- `recovery/` — utility for syncing Cursor IDE conversations
