# tools/

> Placeholder — content lands in commit 3.

Tool-specific configurations, prompts, and integrations. One subdirectory per AI assistant:

- `claude/` — Claude Code (agents, rules, skills, hooks, scaffolding, examples)
- `cursor/` — Cursor IDE (user rules, `.mdc` scoped rules, MCP, recovery utility)
- `chatgpt/` — ChatGPT (global and project-scoped instructions)

For content that applies across all tools, see `shared/` instead.
