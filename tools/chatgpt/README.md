# tools/chatgpt/

> Placeholder — content lands in commit 3.

ChatGPT-specific instructions and prompts.

Planned subdirectories (populated via `git mv` in commit 2):

- `global/` — global custom instruction sets
- `projects/` — project-scoped instruction sets
