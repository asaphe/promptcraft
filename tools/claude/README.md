# tools/claude/

> Placeholder — content lands in commit 3.

Claude Code configurations — for users adapting patterns to their own projects.

Planned subdirectories (populated via `git mv` in commit 2):

- `guides/` — design guides and reference material
- `templates/` — starter templates for agents, skills, rules, projects
- `scaffolding/` — a complete example `.claude/` directory
- `examples/` — production implementations from real projects

For Claude Code contributor instructions for *this* repo, see `.claude/` at the repo root.
