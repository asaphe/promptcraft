# shared/workflows/

CI/CD and automation patterns — tool-agnostic.

## Contents

- `ci-cd-patterns.md` — universal CI/CD discipline (pinning, idempotence, stop-on-first-failure).
- `github-actions.md` — GitHub Actions specifics (OIDC, reusable workflows, composite actions, `always()`, concurrency).
- `version-management.md` — single-source-of-truth for package and tool versions across CI, containers, and deployments.
